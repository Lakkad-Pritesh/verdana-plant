import React from 'react';
import { ProductGrid } from '../components/ProductGrid';

export const Shop: React.FC = () => {
  return (
    <div className="pt-15">
      <ProductGrid />
    </div>
  );
};
